# Data & model artifacts (keep folder structure via .gitkeep)
data/raw/*
data/processed/*
models/*
*.pth
*.pt
*.ckpt
!**/.gitkeep
!**/*.dvc

# Experiment tracking (MLflow sqlite backend + local artifacts)
mlflow.db
mlruns/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
venv/
env/

# Notebooks
.ipynb_checkpoints/

# Logs & runs
runs/
logs/
*.log

# Experiment tracking (MLflow) & data splits artifacts
# Anchored to repo root so it only ignores the results dir, NOT config/experiments/.
/experiments/
mlruns/
*.db

# OS / editor
.DS_Store
.idea/
.vscode/

# Jury grading bundle (built by make jury-bundle, hosted on public Drive)
jury_bundle.tar.gz
